build:
  docker:
    web: Dockerfile.heroku
    app:
      dockerfile: Dockerfile.heroku
      target: builder 
release:
  image: app
  command:
    - goose up -dir ./app/db/migrations
run:
  web: /main