build:
  docker:
    web: Dockerfile.heroku
    app:
      dockerfile: Dockerfile.heroku
      target: builder 
release:
  image: app
  command:
    - cd app/db/migrations && goose up
run:
  web: /main